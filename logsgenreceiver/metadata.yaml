type: logsgen

status:
  class: receiver
  stability:
    development: [logs]
  distributions: [contrib]
  codeowners:
    active: [alexsapran]

tests:
  config:
    interval: 30000
    scenarios:
      - path: builtin/simple
        scale: 1
        logs_per_interval: 1
